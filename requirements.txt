# CPU deploy (Docker Space). All inference goes through the llama-server binary that the
# Dockerfile compiles from llama.cpp source with AVX2 (the prebuilt CPU wheels/binaries are
# un-vectorized; AVX2 gives ~60-80 tok/s prefill / ~12 decode on cpu-basic). The app talks
# to it over HTTP — no llama-cpp-python, no GPU, no CUDA anywhere.

# --- Gradio app (launched by our own CMD inside the Docker Space) ---
gradio==6.0.1

# --- HTTP client for the managed llama-server subprocess ---
requests

# --- Hub access for pulling the base GGUF + director/style LoRAs at startup ---
huggingface_hub
