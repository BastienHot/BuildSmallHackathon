# CUDA 12.1 wheels — targets ZeroGPU (A10G), HF T4, and consumer GPUs (RTX 20+).
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121

# --- Gradio app (HF Space SDK: gradio, ZeroGPU hardware) ---
gradio==6.0.1

# --- ZeroGPU GPU allocation decorator (transparent no-op on T4 / local GPU) ---
spaces

# --- Text models via llama.cpp (CUDA 12.1 build) ---
llama-cpp-python==0.3.26

# --- TTS: VoxCPM2 (PyTorch). torch also used for CUDA detection at startup. ---
torch
# voxcpm            # uncomment once the exact package/source is confirmed
soundfile
numpy

# --- Hub access for pulling GGUFs / LoRAs / voice refs ---
huggingface_hub
